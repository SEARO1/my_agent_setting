# 敏感檔案 — 真 API key 永遠唔好 commit
.credentials.yaml
*.key
*.pem
.env

# 本地 runtime 產物
.dsh-vision-router/
*.log
node_modules/

# 舊 zip（而家用 GitHub 唔用 zip）
*.zip
_trash/

# 会話 transcript（私隱，不 commit）
session_*.jsonl

# 舊 KB 向量索引（可重建，舊 mcp-knowledge 已移除）
kb-index.json
